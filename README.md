# https-localhost is a 2 command https server

# open a terminal in this directory
	npm i -g --only=prod https-localhost
	serve ./

# if not in this directory, use:
	serve ./path/to/directory

# access website at https://localhost:443